# Если эта опция выключена, то будут компилироваться только те библиотеки, которые используются в каких-то таргетах
option(dviglo_build_all_libs "Компилировать даже те библиотеки, которые не используются в каких-то таргетах" OFF)

# Добавляем все подпапки
dviglo_add_all_subdirectories()
